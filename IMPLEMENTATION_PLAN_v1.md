# SensiDoc 敏感信息提取与高亮预览工具 — 实施方案 (v1)

> 项目目标：提供开箱即用的离线文档敏感信息识别工具，基于 `anydoc` 实现全格式文档解析，通过项目内置的 `llama.cpp` 驱动本地离线小模型（支持魔搭 ModelScope 一键拉取与本地加载），在符合 Geist-Neutral 规范的 Web 界面中实现 Markdown 渲染、敏感词上下文联动高亮与一键导出。

---

## 1. 架构总览与技术选型

### 1.1 核心技术选型

| 层次 | 选型组件 | 选型理由与考量 |
|------|---------|---------------|
| **后端服务** | Rust (Axum + Tokio) | ① 极轻量，毫秒级启动，常驻内存仅 ~15MB；② 原生异步与高吞吐流式（SSE）传输；③ 纯本地代码，无额外 Python/Node 环境依赖。 |
| **文档转换引擎** | `anydoc` (Rust 原生 Crate) | 直接依赖 `/Users/icychick/项目学习/anydoc` 本地 Crate，零胶水层与零 FFI 损耗，纳秒级解析 Word、PDF、Excel、PPT 等为标准 GFM Markdown。 |
| **推理运行时** | `llama.cpp` (`llama-server`) | 内置 macOS Apple Silicon 二进制到项目 `bin/` 目录，启用 Metal GPU 加速（`-ngl 99`），对外暴露标准 OpenAI 兼容接口，由 Rust 主进程统一拉起与生命周期守护。 |
| **模型源与预设** | 魔搭 ModelScope API + GGUF 直链 | 预设三款轻量模型（LFM2-350M-Extract、LFM2.5-VL-450M、Qwen2.5-1.5B），Rust 端使用 `reqwest` 流式异步下载并输出 SSE 进度。 |
| **前端界面** | Web 静态应用 (Vue 3 + Geist-Neutral CSS) | 现阶段由 Axum 提供静态目录映射（`tower-http`），便于快速修改与刷新验证；后续分发时可一键改用 `rust-embed` 打包为单文件。严格遵循 `DESIGN_SYSTEM-v1.md` 规范。 |
| **渲染与联动** | Markdown 渲染器 + AST 敏感词高亮插桩 | 将 Markdown 渲染为 HTML 后，精准注入 `<mark class="sensi-mark">`，与右侧敏感词看板实现双向滚动聚焦。 |

---

## 2. 系统拓扑与数据流向

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                          用户浏览器前端 (Web UI)                             │
│  [文档上传 / 拖拽]       [魔搭模型一键下载]       [Markdown预览+高亮]    [一键导出]  │
└───────┬─────────────────────────┬─────────────────────────▲─────────────────┘
        │ POST /api/convert-parse │ GET /api/models/download│ (SSE/JSON 渲染)
        ▼                         ▼                         │
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Rust 后端服务 (SensiDoc Server: Axum)                 │
│                                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌────────────────────┐ │
│  │ 1. anydoc 原生调用   │  │ 2. 模型下载与管理器  │  │ 3. 结果对齐与高亮  │ │
│  │ docx/pdf/xlsx -> md  │  │ reqwest 流式 -> models│  │ 词频统计 / 坐标定位│ │
│  └──────────┬───────────┘  └──────────┬───────────┘  └─────────▲──────────┘ │
│             │                         │                        │            │
│             ▼                         ▼                        │            │
│  ┌──────────────────────┐  ┌──────────────────────┐            │            │
│  │ Prompt 模版组装      │  │ llama-server 进程守护 │            │            │
│  │ (实体识别+正则兜底)  │  │ Metal 加速 / 端口管理 │            │            │
│  └──────────┬───────────┘  └──────────┬───────────┘            │            │
└─────────────┼─────────────────────────┼────────────────────────┼────────────┘
              │                         │                        │
              ▼                         ▼                        │
       ┌──────────────┐          ┌──────────────┐                │
       │ Markdown 文本 │ ───────► │ llama.cpp    │ ───────────────┘
       └──────────────┘          │ 离线推理引擎 │  (返回结构化 JSON:
                                 └──────────────┘   实体、分类、风险等级)
```

---

## 3. UI 原型设计稿 (Text UI Wireframe)

按照 `DESIGN_SYSTEM-v1.md` 规范：TopBar (40px) + 三栏工作区（左侧文件区 260px、中间预览与规则配置区 Flex 1、右侧提取明细 280px）。
- **左侧**：顶部为**文件投放区**（支持点击多选与拖拽多文件），下方为**文件树列表**（每个文件可折叠展开**历史提取记录快照**）。
- **中间上方**：**敏感信息规则与提示词定义区**（支持选择场景模板、结构化表格配置、调整描述快速提取测试准确率、一键折叠/展开）。
- **设置面板**：点击 TopBar 右上角 `[⚙ 设置]` 弹出**离线模型管理模态框**。

### 3.1 主界面（展开规则配置与历史快照）
```text
╔═══════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║ SensiDoc  │  ● LFM2-350M-Extract (就绪)                               [ 查找文本... ]  [ 导出结果 ▼ ] [⚙ 设置]║
╠══════════════════════╦════════════════════════════════════════════════════════════════╦═══════════════════════╣
║ 📁 文档与历史记录    ║ 📄 2026战略合作协议.docx (已转MD · 142 KB)                     ║ 🏷️ 敏感项明细 (共 6 处)║
╠══════════════════════╬════════════════════════════════════════════════════════════════╬═══════════════════════╣
║ ┌──────────────────┐ ║ ┌─ 🛠️ 提取目标与提示词配置 (Prompt Schema) ─────── [▲ 收起配置] ┐ ║ 筛选: [全部(6)] [高危]║
║ │ 拖拽多文件到此处 │ ║ │ 场景模板: [ 🏢 商业合作与合规审查 ▼ ]  [ 恢复默认 ]          │ ║                       ║
║ │ 或 [点击选择文件]│ ║ │ ┌────────────┬─────────────────────────────┬────────┬──────┐ │ ║ ┌───────────────────┐ ║
║ └──────────────────┘ ║ │ │ 字段名称   │ 提取规则描述 (传递给AI模型) │正则增强│ 操作 │ │ ║ │ 绝密研发指标 [高危]│ ║
║ 批量管理: [清空列表] ║ │ ├────────────┼─────────────────────────────┼────────┼──────┤ │ ║ │ 量子退火参数      │ ║
║                      ║ │ │ 身份证件   │ 18位自然人法定居民身份证号码│ [ 开 ] │ [删] │ │ ║ │ 出现 1 次 · 第12行 │ ║
║ ▼ 合作协议.docx (3)  ║ │ │ 移动电话   │ 11位中国大陆手机与联系方式  │ [ 开 ] │ [删] │ │ ║ └───────────────────┘ ║
║   ├ 🕒 15:30 (最新)● ║ │ │ 绝密指标   │ 未公开技术代号与物理退火参数│ [ 关 ] │ [删] │ │ ║ ┌───────────────────┐ ║
║   │   6处风险·准确率高║ │ └────────────┴─────────────────────────────┴────────┴──────┘ │ ║ │ 身份证号   [高危] │ ║
║   ├ 🕒 14:15 快照A   ║ │ [+ 新增字段]                      [ 🚀 立即提取 (测试准确率) ]│ ║ │ 110101199003072345│ ║
║   │   4处风险·默认模板║ └──────────────────────────────────────────────────────────────┘ ║ │ 出现 2 次 · 正文P1│ ║
║   └ 🕒 11:20 快照B   ║ # 2026 年度技术合作框架协议                                    ║ └───────────────────┘ ║
║       2处风险·仅PII  ║ 甲方：未来智能科技有限公司                                     ║ ┌───────────────────┐ ║
║                      ║ 法定代表人：<mark class="danger">张伟</mark>                   ║ │ 手机号码   [中危] │ ║
║ ▶ 员工薪酬表.xlsx    ║ 签署人身份证号为：`<mark class="danger">110101199003072345</mark>`║ │ 13800138000       │ ║
║   └ (点击展开历史)   ║ 项目保密技术要点包含自研的`<mark class="danger">量子退火参数</mark>`║ │ 出现 3 次 · 正文P2│ ║
╚══════════════════════╩════════════════════════════════════════════════════════════════╩═══════════════════════╝
```

### 3.2 规则收起态（最大化正文审计沉浸浏览）
```text
║ 📁 文档与历史记录    ║ 📄 2026战略合作协议.docx (142 KB)                              ║ 🏷️ 敏感项明细 (共 6 处)║
╠══════════════════════╬════════════════════════════════════════════════════════════════╬═══════════════════════╣
║ ▼ 合作协议.docx (3)  ║ [● 模板:商业合作审查 (身份证件/移动电话/绝密指标 3字段)] [▼ 展开配置] [🚀 重新提取]  ║ ┌───────────────────┐ ║
║   ├ 🕒 15:30 (当前)● ║ ────────────────────────────────────────────────────────────── ║ │ 绝密研发指标 [高危]│ ║
║   ├ 🕒 14:15 快照A   ║ # 2026 年度技术合作框架协议                                    ║ │ 量子退火参数      │ ║
```

### 3.3 点击右上角 `[⚙ 设置]` 弹出的模型管理模态框
```text
         ┌───────────────────────────────────────────────────────────────┐
         │ ⚙️ 离线推理模型管理                                       [X] │
         ├───────────────────────────────────────────────────────────────┤
         │ 本地运行时: ./bin/llama-server (Apple Silicon Metal GPU 加速) │
         │ 服务状态: ● 运行中 (PID: 34102 · Port: 8081)                 │
         │                                                               │
         │ 预设模型库 (魔搭 ModelScope 直链):                            │
         │ ┌───────────────────────────────────────────────────────────┐ │
         │ │ ● LiquidAI / LFM2-350M-Extract (Q8_0)            [ 使用中 ]│ │
         │ │   专为信息抽取调优 · 体积 380MB · 极低显存                 │ │
         │ ├───────────────────────────────────────────────────────────┤ │
         │ │ ○ LiquidAI / LFM2.5-VL-450M (Q4_K_M)        [ 一键下载 ]   │ │
         │ │   多模态视觉小模型 · 体积 450MB                           │ │
         │ ├───────────────────────────────────────────────────────────┤ │
         │ │ ○ Qwen / Qwen2.5-1.5B-Instruct               [ 正在下载... ]│ │
         │ │   进度: 68% (1.1GB/1.6GB) · 速度: 18.5 MB/s               │ │
         │ │   [▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░]                              │ │
         │ └───────────────────────────────────────────────────────────┘ │
         │                                                               │
         │ 本地模型载入:                                                 │
         │ 路径: [/path/to/custom_model.gguf                    ] [ 载入 ]│
         │                                                               │
         │                                                 [  关闭 (ESC)  ]│
         └───────────────────────────────────────────────────────────────┘
```

---

## 4. 关键模块详细设计

### 4.1 anydoc 文档转换引擎封装
- **调用逻辑**：
  直接在 `Cargo.toml` 中引用本地 Crate，零胶水层：
  ```toml
  [dependencies]
  anydoc = { path = "/Users/icychick/项目学习/anydoc" }
  ```
  Rust 处理逻辑：
  ```rust
  // 从上传的字节直接自动推断格式并转换为标准 Markdown
  let markdown_text = anydoc::to_markdown_bytes(&bytes, None)
      .map_err(|e| format!("解析失败: {}", e))?;
  ```
- **格式覆盖**：Word (`.doc`, `.docx`)、PowerPoint (`.pptx`)、Excel (`.xlsx`, `.xls`)、PDF (`.pdf`)、OpenDocument (`.odt`)、RTF、EPUB、CSV。
- **降级容错**：若遇到加密或损坏文件，捕获 `anydoc::ConvertError` 并向前端反馈友好的 Geist 风格提示。

### 4.2 llama.cpp 运行时与模型管理
- **内置二进制**：
  项目根目录下创建 `bin/` 目录，将 macOS Apple Silicon 版 `llama-server` 内置到 `bin/llama-server`。
  Rust 服务启动前自动检测并赋予执行权限。
- **魔搭 ModelScope 预设模型下载**：
  预设 3 个小模型配置项（基于 ModelScope 官方文件解析直链下载），使用 `reqwest` 异步获取字节流，通过 Axum Server-Sent Events (SSE) 实时向前端推送下载进度（支持 Range 断点续传与 SHA256 校验）。
- **进程生命周期与防孤儿机制**：
  切换模型或停止服务时，Rust 端通过 `tokio::process::Child` 优雅终止进程。为防主进程崩溃导致僵尸进程持续占用 8081 端口与显存，必须实现**全局信号捕获（SIGINT/SIGTERM）**并在 `Drop` trait 中执行强杀（kill），确保资源绝对释放。

### 4.3 敏感信息提取引擎 (混合模型 + 分块策略 + 正则兜底)
- **长文本分块策略 (Chunking)**：
  文档转换后的 Markdown 极易超过小模型的 `-c 4096` Token 限制。后端必须引入分块机制，按段落/换行符将文本切分为约 2500 Tokens 的切片，依次或并行送入 LLM 推理，最终在 Rust 端将全部结果合并并重排。
- **场景预设模板 (Presets)**：
  - 【商业合作与合规审查】：包含商业机密、绝密技术指标、财务结算账号、违约赔偿金等。
  - 【个人隐私信息保护 (PII)】：包含身份证件、手机电话、现住址、电子邮箱、护照号。
- **冲突消解算法**：
  当正则兜底匹配（如 11 位手机号）与 LLM 语义识别的片段在文本偏移量（Offset）上产生重叠时，执行冲突消解，优先保留精确的正则边界或高风险等级项。

### 4.4 批量文件投放与历史快照持久化
- **文件投放与解析**：支持拖放多文件 / 点击多选，异步调用 `anydoc` 解析。
- **快照持久化 (Persistence)**：
  - 每个文件维护一个历史版本树：`Vec<ExtractionSnapshot>`。
  - 数据不仅保存在内存中，还需**通过 `sled` 或本地 JSON 文件（如 `.sensidoc_workspace.json`）落盘持久化**。重启应用后，历史审计记录不丢失。

### 4.5 文本高亮插桩与联动预览
- 采用双层处理：
  1. 前端渲染 Markdown 为 HTML。
  2. ⚠️ **精准 DOM 遍历**：不使用暴力的 `String.replace` 破坏 HTML 结构，必须采用 `TreeWalker` API 仅遍历并处理纯文本节点（`Node.TEXT_NODE`），将敏感词精准包裹为：`<mark class="sensi-mark" data-id="xxx">敏感词</mark>`。
- **联动交互**：
  - 点击右侧敏感词卡片 -> 若卡片包含同词的多个坐标，则依次循环轮转滚动跳转，触发两次背景脉冲闪烁（Flash 动画）。

### 4.6 一键导出
- 支持多种格式导出：
  1. **敏感词清单 (CSV / JSON)**：包含【敏感词内容、类型、风险等级、频次、段落、对应快照时间】。
  2. **脱敏文档 (Markdown / TXT)**：一键对原文进行打码替换并下载。

---

## 5. 项目工程结构规划

```text
SensiDoc/
├── bin/                       # 内置 llama.cpp 运行时 (macOS arm64 可执行程序)
│   └── llama-server
├── models/                    # 离线模型存放目录 (.gguf)
├── web/                       # Web 静态前端应用 (直接热修改、热测试)
│   ├── index.html             # 遵循 DESIGN_SYSTEM-v1 的极简单页 (多文件投放+折叠规则+设置弹窗)
│   ├── app.js                 # 交互逻辑、状态流、Markdown 渲染、快照回溯与双向高亮联动
│   └── style.css              # Geist-Neutral 设计系统 Token 样式表
├── src/                       # Rust 后端服务
│   ├── main.rs                # Axum 路由主入口与静态资源服务
│   ├── converter.rs           # anydoc 原生调用层
│   ├── model_manager.rs       # llama-server 进程守护 (防僵尸) 与下载器
│   ├── extractor.rs           # 分块 (Chunking) 抽取引擎与冲突消解
│   ├── session.rs             # 多文档管理与历史快照持久化 (落盘)
│   └── exporter.rs            # 导出与脱敏处理
├── Cargo.toml                 # Rust 项目依赖配置
├── run.sh                     # 一键启动测试脚本 (cargo run)
├── ARCHITECTURE_REVIEW.md     # 架构师审查评估报告
└── IMPLEMENTATION_PLAN_v1.md  # 实施方案与设计文档
```

---

## 6. 实施路线图 (Milestones)

- [ ] **M1: Rust 基座与 anydoc 解析**
  - 初始化 Rust Cargo 工程，链接本地 `anydoc` 库，内置 `llama-server`，实现严格的进程生命周期守护。
- [ ] **M2: 魔搭 ModelScope 在线下载器**
  - Rust 实现预设模型的一键流式拉取（断点续传/SSE通知）与热管理，完成设置模态框。
- [ ] **M3: 混合敏感信息提取与快照落盘**
  - 实现长文本分块 (Chunking) 策略、LLM 异步推理、正则冲突消解，完成历史快照文件本地持久化。
- [ ] **M4: Geist-Neutral 网页端界面构建**
  - 完成三栏布局、文件多选/拖放、历史快照树、可折叠规则表格，基于 `TreeWalker` 实现安全的双向脉冲高亮。
- [ ] **M5: 导出功能与整机联调**
  - 实现 CSV/JSON 导出与打码脱敏，进行多文件批处理与长文本边界测试验证。


