# SensiDoc 架构与实施方案评估报告 (ARCHITECTURE_REVIEW.md)

## 1. 方案亮点 (The Good)

*   **极简高效的基础栈**：选用 Rust (Axum) 作为基座，内存占用极低（~15MB），结合本地 `anydoc` 零 FFI 损耗，非常契合“离线、开箱即用”的定位。
*   **进程级解耦的 AI 引擎**：将 `llama-server` 作为独立子进程由 Rust 守护，既享受了 C++ 原生极致的推理性能与 Metal GPU 加速，又避免了直接链接 C++ 库带来的内存不安全和编译复杂性。
*   **交互闭环清晰**：从文件投放 -> 动态提示词组装 -> 混合提取 -> 历史快照复核 -> 脱敏导出，业务链路完整，逻辑自洽；历史快照（Snapshot）设计对于合规审计尤为贴心。
*   **免环境依赖**：统一打包 Rust 后端与 llama-server 二进制，配合 Web 静态资源打包（未来计划 `rust-embed`），彻底消除了 Python/Node.js 等环境配置门槛。

## 2. 潜在风险与优化建议 (Risks & Recommendations)

### 2.1 架构可行性与健壮性 (Architecture)
*   **进程守护与僵尸进程风险**：Rust 使用 `tokio::process::Child` 拉起 `llama-server` 时，如果主进程（Axum）发生 Panic 或被意外终止（如 `kill -9`），可能会导致 `llama-server` 变成孤儿/僵尸进程，持续占用端口 `8081` 和宝贵的显存资源。
    *   *建议*：在 Rust 端实现全局的信号捕获（SIGINT/SIGTERM），并在 `Drop` trait 中显式清理（kill）子进程。重启切换模型时，加入端口探测与重试机制，确保旧进程彻底释放资源。
*   **大文件处理的上下文瓶颈 (Chunking Strategy)**：文档转 Markdown 后，长度极易超过模型设置的 `-c 4096` Token 限制。直接整段送入 LLM 会导致截断、回答不完整或引发 OOM。
    *   *建议*：必须在 `extractor.rs` 中引入**文本分块机制 (Chunking)**。按段落或 Token 数量（如每 2500 Tokens）对 Markdown 进行切片分发，随后在 Rust 端将多次提取的 JSON 结果进行合并与去重。
*   **模型下载的可靠性**：数百 MB 至 GB 级的模型文件在弱网环境下容易中断。
    *   *建议*：基于 `reqwest` 实现断点续传（基于 HTTP Range），下载完毕后执行 SHA256/MD5 校验，避免加载不完整的模型文件导致 `llama.cpp` 崩溃。

### 2.2 功能完备性 (Functionality)
*   **快照状态的持久化盲区**：方案提到数据保存在 `session.rs` 的“缓存”中。若为纯内存管理，服务重启后将丢失所有文档记录与快照比对能力。
    *   *建议*：引入轻量级本地数据库（如 `SQLite`、`Sled`）或文件系统落盘（存储为 JSON），实现 Snapshot 和用户配置项的持久化。
*   **LLM 幻觉与正则结果冲突**：混合抽取时，LLM 和正则可能会匹配到同一段文本的重叠区域（例如正则圈出 11 位手机号，LLM 圈出包含该号码的半句话），导致结果列表出现冗余和交叉高亮异常。
    *   *建议*：在提取引擎层增加“位置冲突消解算法”。计算文本偏移量（Offset），若发现重叠，优先保留正则结果，或以风险等级高的作为主导。

### 2.3 用户体验与 UI 交互 (UI/UX)
*   **同词多处出现的焦点轮转**：右侧列表显示某个敏感词“出现 3 次”。目前的交互是“点击触发一次跳转与背景脉冲”。
    *   *建议*：若卡片包含多个坐标，点击卡片应当在这 3 个位置之间轮转滚动跳转（例如：1/3 -> 2/3 -> 3/3），并且配套提供左右小箭头进行微调定位。
*   **历史快照的沉浸感知**：用户在左侧切换到旧的快照版本时，可能会误认为是当前最新文件。
    *   *建议*：切换至历史快照时，中间预览区顶部应出现“只读横幅”（例如：正在查看 14:15 分的历史快照 [返回最新]），避免用户在此状态下误操作修改规则。
*   **大文件的渲染加载**：
    *   *建议*：遇到超长文档提取时，提供 SSE 逐步返回提取进度的功能，而不是干等接口；在渲染几百个 `<mark>` 标签时，考虑使用虚拟滚动优化性能。

### 2.4 技术实现细节 (Technical Details)
*   **DOM 高亮插桩的致命陷阱**：在渲染好的 HTML 字符串中直接使用正则或 `String.replace` 进行敏感词替换，极易破坏 HTML 原本的标签闭合结构（比如把 `<a class="xxx">` 里的字符串替换成标记）。
    *   *建议*：前端必须使用 `TreeWalker` API 仅遍历并处理纯文本节点（`Node.TEXT_NODE`），或者在 Markdown 编译期间（如编写 `markdown-it` AST 插件）基于精准的行列坐标提前注入 `<mark>`，避免破坏 DOM 结构。
*   **前端模型下载组件**：需要处理 `EventSource` (SSE) 的异常重连机制，当遇到超时或者断网时能够提示用户。

## 3. 结论与 Next Steps (Conclusion)

**总体结论**：当前实施方案在技术选型上极其精当，充分发挥了 Rust 与小模型协同的性能与轻量化优势，UI 交互逻辑顺畅。但在**进程生命周期安全**、**长文档分块策略**以及**DOM 文本精准插桩**三个技术深水区存在一定盲点，必须予以补全。

**Next Steps 建议**：
1. **完善方案**：更新 `IMPLEMENTATION_PLAN_v1.md`，明确补充分块（Chunking）策略与 `TreeWalker` 纯文本节点高亮算法说明。
2. **最小可行性验证 (PoC)**：优先实现 M1 与 M2 模块，特别是要写测试用例验证主进程异常退出后，`llama-server` 也能自动销毁的资源回收机制。
3. **确认存储边界**：明确数据的生命周期。如果刻意不使用数据库，应在界面上给用户显著提醒：“关闭后数据将被清空，请及时导出”。
